<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title><%= title %></title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="stylesheet" href="css/main.css" />
  </head>
  <body>
    <div class="main p-10 w-full min-h-screen bg-zinc-900">
      <h1 class="text-white text-3xl">Notes Taking WebApp</h1>

      <div class="form mt-5 px-10 py-5">
        <form action="/store-note" method="post">
          <input
            name="title"
            class="block text-white rounded-md outline-none px-5 py-2 bg-zinc-800"
            type="text"
            placeholder="Title"
          />
          <textarea
            name="note"
            class="block text-white rounded-md outline-none mt-2 px-5 py-2 bg-zinc-800"
            name=""
            placeholder="write your note here..."
          ></textarea>
          <button
            class="mt-2 pointer-events-auto rounded-md bg-indigo-600 px-3 py-2 text-[0.8125rem] font-semibold leading-5 text-white hover:bg-indigo-500"
            type="submit"
          >
            Save
          </button>
        </form>
      </div>

      <div class="tasks mt-4 flex gap-3 flex-wrap p-10">
        <% if(files.length > 0){ %>
             <% files.forEach((val) => { %>
                <div class="task w-72 px-3 py-4 rounded-md bg-zinc-800">
                    <h3 class="text-white text-2xl tracking-tighter"><%= val.fileName %></h3>
                    <p class="text-white">
                        <%= val.content %>
                    </p>
                    <a class="mt-3" href="/note/<%= val.fileName %>">read more...</a>
                </div>
            <% }) %> 
        <% } else { %>
        <h4>No Task Yet.</h4>
        <% } %>
      </div>
    </div>

    <script src="js/index.js"></script>
  </body>
</html>
